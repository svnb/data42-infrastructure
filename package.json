{
  "name": "infrastructure",
  "main": "index.ts",
  "devDependencies": {
    "@types/node": "^16",
    "prettier": "^3.1.0"
  },
  "scripts": {
    "up": "pulumi up",
    "destroy": "pulumi destroy",
    "prettier": "npx prettier . --write",
    "lint": "npx prettier . --check"
  },
  "dependencies": {
    "@pulumi/kubernetes": "^4.5.5",
    "@pulumi/pulumi": "^3.0.0",
    "@pulumi/random": "^4.14.0",
    "@pulumi/snowflake": "^0.41.0"
  }
}
